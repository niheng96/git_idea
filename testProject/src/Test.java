/*
import designMode.otherTest.single.Single4;

import java.util.*;

public class Test {
    public static void main(String[] args) {
        while(true) {
            System.out.print("输入：");
            Scanner scr = new Scanner(System.in);
            int num = scr.nextInt();
            System.out.println(number(num));
        }
        */
/*HashMap<String, Object> map = new HashMap<>();
        LinkedHashMap<String, Object> linkedHashMap = new LinkedHashMap<>();
        Single4 single4 = new Single4();
        Single4 otherTest.single = new Single4();
        linkedHashMap.put("zhangsan", single4);
        linkedHashMap.put("lisi", otherTest.single);
        Set<Map.Entry<String, Object>> entries = linkedHashMap.entrySet();
        for (Map.Entry<String, Object> entry : entries) {
            String key = entry.getKey();
            Object value = entry.getValue();
            System.out.println(key+value);
        }*//*

//        Object lisi = map.get("lisi");
//        Object zhangsan = map.get("zhangsan");
//        System.out.println(lisi);
//        System.out.println(zhangsan);

    }
    public static int number(int n) {
        if (n < 1) {
            System.out.println("参数无效");
            return 0;
        }
        if (n == 1) {
            return 1;
        } else {
            return n * number(n - 1);
        }
    }

}
*/
